# ============================================
# 특별한 시간 — 설문 조사 환경 설정
# ============================================
# 이 파일을 .env.local 로 복사한 후 값을 채워주세요.
# cp .env.local.example .env.local

# ── Supabase ──────────────────────────────
# https://supabase.com 에서 무료 프로젝트 생성 후
# Settings > API 에서 URL과 anon key를 복사하세요.
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJxxxxx

# ── Admin ─────────────────────────────────
# /admin 페이지 접근 비밀번호
ADMIN_PASSWORD=your_admin_password_here

# ── Supabase 테이블 생성 SQL ──────────────
# Supabase Dashboard > SQL Editor 에서 아래 SQL을 실행하세요:
#
# CREATE TABLE survey_responses (
#   id UUID DEFAULT gen_random_uuid() PRIMARY KEY,
#   survey_type TEXT NOT NULL CHECK (survey_type IN ('a1', 'a2', 'b1', 'b2')),
#   respondent_name TEXT NOT NULL,
#   respondent_contact TEXT,
#   answers JSONB NOT NULL DEFAULT '{}',
#   created_at TIMESTAMPTZ DEFAULT NOW()
# );
#
# CREATE INDEX idx_survey_type ON survey_responses(survey_type);
# CREATE INDEX idx_created_at ON survey_responses(created_at DESC);
